name: Package on main

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-release:
    name: Build release and update packages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Build with ufbt (release)
        id: build
        uses: flipperdevices/flipperzero-ufbt-action@v0.1.2
        with:
          app-dir: ./apps/mouse-jiggler/fap
          sdk-channel: release

      - name: Prepare packages tree
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p packages/apps/Tools packages/subghz packages/badusb packages/nfc
          ART="${{ steps.build.outputs.fap-artifacts }}"
          if [ -d "$ART" ]; then
            FAP=$(ls "$ART"/*.fap | head -n 1)
          else
            FAP="$ART"
          fi
          cp -v "$FAP" packages/apps/Tools/mouse-and-keyboard-jiggler.fap

      - name: Commit packaged app [skip ci]
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          git add -f packages/apps/Tools/mouse-and-keyboard-jiggler.fap
          if git diff --cached --quiet; then
            echo "No package changes to commit"
          else
            git commit -m "chore(packages): update packaged app [skip ci]"
            git push
          fi


